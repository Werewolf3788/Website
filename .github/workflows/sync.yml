# ============================================================================
# File: .github/workflows/fs25-sync.yml
# Deployment Timestamp: 2026-09-01 08:25:12 (EDT - 24hr New York Time)
# Project: entertainment-71888 (/fs25 RTDB Node)
# Description: Smart FS25 Dynamic Savegame Ingestion Workflow.
#              - Triggers every 16 minutes via cron.
#              - Installs xml2js alongside core dependencies.
#              - Synchronizes savegames, farms, and active mods to Firebase.
# ============================================================================

name: FS25 Live Backup Sync

on:
  schedule:
    # Trigger every 16 minutes
    - cron: '*/16 * * * *'
  workflow_dispatch:

concurrency:
  group: fs25-sync
  cancel-in-progress: true

jobs:
  telemetry-sync:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout Code Repository
        uses: actions/checkout@v4

      - name: Setup Node.js Runtime
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: |
          cd games/FS25
          npm install basic-ftp firebase-admin dotenv xml2js

      - name: Run Dynamic Sync Pipeline
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_PORT: ${{ secrets.FTP_PORT }}
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASS }}
          FS25_API_CODE: ${{ secrets.FS25_API_CODE }}
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          DEFAULT_SAVE_SLOT: ${{ secrets.DEFAULT_SAVE_SLOT || '3' }}
        run: |
          cd games/FS25
          node fs25.js
