steps:
  # STREAMING_CHUNK:Checking out the repository...
  - name: Checkout Repository
    uses: actions/checkout@v4
    with:
      fetch-depth: 0 

  # STREAMING_CHUNK:Setting up Node.js environment...
  - name: Setup Node.js
    uses: actions/setup-node@v4
    with:
      node-version: '20'

  # STREAMING_CHUNK:Installing necessary NPM packages...
  - name: Install Dependencies
    run: npm install psn-api

  # STREAMING_CHUNK:Running the PSN synchronization script...
  - name: Run PSN Sync Script
    env:
      PSN_NPSSO_WEREWOLF: ${{ secrets.PSN_NPSSO_WEREWOLF }}
      PSN_NPSSO_RAY: ${{ secrets.PSN_NPSSO_RAY }}
    run: node Playstation/psnscript.js

  # STREAMING_CHUNK:Committing and pushing data changes...
  - name: Commit and Push Data Changes
    run: |
      git config --global user.name "Werewolf3788-Bot"
      git config --global user.email "bot@werewolf.ourflora.com"
      
      git add Playstation/psn_data.json Playstation/tokens.json .nojekyll
      
      if ! git diff --cached --quiet; then
        git commit -m "Automated PSN Sync: $(date)"
        git pull --rebase origin main
        git push origin main
      else
        echo "No data changes detected. Skipping commit."
      fi

  # STREAMING_CHUNK:Deploying to GitHub Pages...
  - name: Setup Pages
    uses: actions/configure-pages@v4

  - name: Upload Artifact
    uses: actions/upload-pages-artifact@v3
    with:
      path: '.'

  - name: Deploy to GitHub Pages
    id: deployment
    uses: actions/deploy-pages@v4
